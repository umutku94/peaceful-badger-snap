import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.peacefulbadgersnap',
  appName: 'peaceful-badger-snap',
  webDir: 'dist'
};

export default config;
